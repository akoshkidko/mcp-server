# Demo project Python dependencies.
# Mix of pinned and unpinned to demonstrate dependency risk detection.

# Pinned — safe
requests==2.28.0

# Unpinned — version risk: any version will be installed
tenacity

# Unpinned with lower bound — still risky
flask>=2.0
